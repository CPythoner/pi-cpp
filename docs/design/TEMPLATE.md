# 设计文档模板（文章级）

> **用法**：每个版本动代码前，复制本模板为 `docs/design/vX.Y.Z.md` 并填充。
> **双重要求**：内部要「照着能写代码」，外部要「删掉附录即可发表」。
> 写作基准：读者是无上下文的 C++ 开发者；「为什么」优先于「做什么」。

---

# 用 C++ 重写 pi coding agent（N）：vX.Y.Z <主题>

> 状态：草案 ｜ 实现中 ｜ 已完成（tag `vX.Y.Z`）
> 系列导航：上一篇 ｜ 下一篇 ｜ [项目总览](../dev-plan.md) ｜ [pi 架构参考](../pi-architecture.md)

## 0. 本篇位置

（~150 字）pi-cpp 是什么——用 C++17 重写 [pi](https://github.com/earendil-works/pi)（一个极简主义 coding agent）的项目；写到哪里了；本篇解决什么问题。配一张路线图小表，标粗当前位置：

| 版本 | 主题 | 状态 |
|---|---|---|
| … | … | ✅ |
| **vX.Y.Z** | **本篇主题** | 👈 |
| … | … | ⏳ |

## 1. 背景与动机

- 要解决的问题是什么？没有这个版本会怎样？
- pi 是怎么解决的？（引用 pi 源码文件+行号，或 pi-architecture.md 相应章节）
- 为什么现在做（依赖关系）？

## 2. 目标与非目标

**目标**（可验证）：
- …

**非目标**（明确不做，为什么）：
- …

## 3. 原理讲解（学习篇）

本版本涉及的核心机制，讲透再动手：

### 3.1 pi 侧的机制
（例：JSONL 树状会话的 append-only 语义、SSE 跨 chunk 分帧、partial message 原地替换不变量……引用 `pi-architecture.md` 章节与 pi 源码行号）

### 3.2 C++17 技术点
（本版本用到什么：variant/visit？条件变量与取消令牌？移动语义？——为什么用、怎么用、已知的坑。学习型项目的核心章节，宁可详细）

## 4. 架构设计

- 模块图（ASCII）+ 数据流走查
- 本版本新增/修改的模块与既有模块的关系
- 线程模型（若有并发）

## 5. 详细设计

### 5.1 文件清单

| 文件 | 职责 | 关键接口 |
|---|---|---|

### 5.2 核心数据结构与接口

（C++ 代码块，含注释；variant 定义给出完整的 using 声明）

### 5.3 wire / 存储格式

（JSON 示例直接抄蓝本真实样本；camelCase 别名表）

### 5.4 关键算法 / 状态机

（逐步描述，必要时伪代码）

## 6. 设计决策与权衡

每条决策一节：**问题 → 候选方案 → 选择与理由 → pi/tau 对照**。

（示例格式）
### 决策 1：<标题>
- 问题：…
- 候选：A / B / C
- 选择：A。理由：…
- pi 的做法：<路径:行号>；偏差与原因：…

## 7. 测试方案与验收

- 测试策略（单测/集成/黄金样本/FakeProvider 脚本）
- 关键用例清单
- 验收命令与期望输出

## 8. 总结与下篇预告

（3-5 句；下一篇要解决什么）

---

## 以下为内部附录（发布文章时删除）

### 附录 A：任务拆解

（writing-plans 产物：TDD 任务列表，checkbox 跟踪）

### 附录 B：验收 checklist

- [ ] （从 dev-plan.md 对应版本复制并细化）

### 附录 C：参考蓝本对照表

| 本仓库文件 | pi 蓝本 | tau 蓝本 | 备注 |
|---|---|---|---|

### 附录 D：实现回顾（完成后追加）

- 实际与设计的偏差及原因
- 踩坑记录（编译器差异、平台差异、协议细节）
- 遗留问题 → 转入下版本
